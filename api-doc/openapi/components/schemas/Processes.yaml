type: object
properties:
  timestamp:
    type: string
    description: Timestamp of measurement
    format: date-time
  processes:
    type: string
    description: JSON encoded information about processes
